# /etc/lost-minutes/caddy.env, read by the Caddy drop-in (deploy/systemd/caddy.service.d/).
# The public name of the site: its DNS A record must point at the server before Caddy starts,
# so that it can obtain the certificate.
LM_DOMAIN=lostminutes.example.co.uk
LM_ROOT=/srv/lost-minutes/app
