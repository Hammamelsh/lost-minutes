.git/
.venv/
node_modules/
.next/
test-results/
playwright-report/
# Anchored with a leading slash: an unanchored "data/" also matched public/data/, which is the
# published catalogue, so the server would have had no stops, no patterns, no replay and no road
# shapes. The warehouse and the raw captures are what this means to exclude.
/data/
.env
.env.local
.env.bak*
# Gitignored and large: the local package cache and the probes' videos and frames are 1.5 GB and
# 280 MB here, and the server neither builds nor reads them. Without these the upload is a few MB.
/.pnpm-store/
/outputs/
*.tsbuildinfo
/.claude/
# Written on the server by the collector and the status job: never overwritten by a deploy.
public/data/live.json
public/data/config.json
public/data/operations.json
# Rebuilt nightly on the server from what the server itself has observed (lost-minutes-refresh).
# On 21 September 2026 a deploy put this machine's three-day-old copy over that morning's rebuild;
# publish.sh now sends it only to a server that has none yet (a first install).
public/data/patterns.json
# Written nightly on the server by lost-minutes-arrival-eval; a deploy's --delete removed it
# (found 21 September: served 404 after the 16:15 deploy, written again at 03:12).
public/data/arrival-release.json
# Built nightly on the server beside the catalogue (lost-minutes-refresh): one scheduled departure
# board per boarding point, and the operating rules they share. About 23 MB here; sending it would
# put this machine's timetable over the server's own, which is the fault of 21 September 2026 again.
public/data/departures/
public/data/departures.json
public/data/departure-rules.json
